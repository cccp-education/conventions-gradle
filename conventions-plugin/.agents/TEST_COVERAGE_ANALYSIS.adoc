= Test Coverage Analysis — Conventions Gradle Plugin
:description: Suivi de la couverture de tests
:session: 000

== Session 000

Aucun test ajoute. Session de gouvernance uniquement.

== Session 001

**Functional tests (GradleTestKit)** : 3 tests
- GradlePluginConventionsFunctionalTest
  - `plugin applies without error` : ✅
  - `plugin registers test task` : ✅
  - `plugin does not fail on check` : ✅

**Cucumber BDD** : 3 scenarios
- `Plugin applies required plugins` : ✅ (3 Thens : java-gradle-plugin, kotlin-jvm, maven-publish)
- `Plugin configures Java compatibility` : ✅ (4 Thens : source 24, target 24, sourcesJar, javadocJar)
- `Plugin configures test tasks` : ✅ (2 Thens : JUnit platform, testLogging)

**Couverture** : US-1.1→US-1.4 couverts. US-1.5 fixAnnotationsConflict et US-1.6 koverEnabled non implementes.

== Session 002

**Functional tests (GradleTestKit)** : 3 tests
- PublishingConventionsFunctionalTest
  - `plugin applies without error` : ✅
  - `plugin registers generatePom task` : ✅
  - `plugin generates pom with developers` : ✅

**Cucumber BDD** : 3 scenarios
- `Publishing plugin applies POM metadata` : ✅ (4 Thens : id, name, license, SCM)
- `Publishing plugin signs publications conditionally` : ✅ (2 Thens : signing applied, signed)
- `Publishing plugin supports relocation` : ✅ (2 Thens : group, artifact)

**Couverture** : US-2.1→US-2.4 couverts. 6/6 functional + 6/6 cucumber = 12 tests PASS.

== Session 003

**Functional tests (GradleTestKit)** : 4 tests
- FunctionalTestConventionsFunctionalTest
  - `plugin applies without error` : ✅
  - `plugin registers functionalTest source set` : ✅
  - `plugin registers functionalTest task` : ✅
  - `plugin wires check depends on functionalTest` : ✅

**Cucumber BDD** : 3 scenarios
- `Plugin creates functionalTest source set` : ✅
- `Plugin registers functionalTest task` : ✅
- `Plugin wires check task dependency` : ✅

**Couverture** : US-3.1→US-3.4 couverts. US-3.5 (sourceSetName, additionalDependencies) P2 implemente mais pas teste en Cucumber. 10/10 functional + 9/9 cucumber = 19 tests PASS cumules.

== Session 004

**Functional tests (GradleTestKit)** : 4 tests
- CucumberConventionsFunctionalTest
  - `plugin applies without error` : ✅
  - `plugin registers cucumberTest task` : ✅
  - `plugin configures features and scenarios dirs` : ✅
  - `plugin wires check depends on cucumberTest` : ✅

**Cucumber BDD** : 4 scenarios
- `Plugin creates features and scenarios source dirs` : ✅
- `Plugin registers cucumberTest task` : ✅
- `Plugin excludes scenarios from test task` : ✅
- `Plugin wires check task dependency` : ✅

**Migration** : src/ deplace de la racine git vers conventions-plugin/ (architecture conforme AGENT.adoc)

**Couverture** : US-4.1→US-4.5 couverts. 14/14 functional + 13/13 cucumber = 27 tests PASS cumules.

== Session 005

**Architecture uniquement** — Aucun test modifie.

Changement structurel : suppression `include()` du root settings, `conventions-plugin/` devient
un build Gradle independant avec son propre wrapper, catalog, settings.

Root devient un consommateur dogfood qui applique `education.cccp.build.cucumber` depuis `mavenLocal()`.

**Validation** : `./gradlew check` depuis conventions-plugin/ — 12 tasks BUILD SUCCESSFUL.
`./gradlew tasks` depuis root/ — BUILD SUCCESSFUL (plugin resolu depuis mavenLocal).

== Session 006

**Migration dogfood uniquement** — Aucun test modifie.

Test des conventions via 3 projets consommateurs :
- dashboard-gradle : 34 tests PASS ✅
- capsule-gradle : functional tests OK ✅
- codebase-gradle : compileKotlin + compileTestKotlin OK ✅

**Bugs corrigés** (sessions 005-006) :
- FunctionalTestConventionsPlugin : source set dans `apply()` (fix `InvalidPluginMetadataException`)
- `gradleTestKit()` shorthand remplace Maven coordinate
- capsule-gradle : `afterEvaluate` workaround pour `extendsFrom(testImplementation)`

== Session 008

**Publication uniquement** — Aucun test modifié.

Validation de la chaîne de publication :
- `check` ✅ (pas de régression — 12 tasks, baseline verte conservée)
- `publishToMavenLocal` ✅ (5 publications signées GPG)
- `publishAggregationToCentralPortal` ✅ (bundle Maven Central)
- `publishPlugins` ✅ (4 plugins Plugin Portal)

**Couverture** : inchangée — 14/14 unit + Cucumber, 15/15 functional PASS.

== Session 010

**CNV-7 Centralisation deps test** — 3 US (8 pts) en baby-step TDD/BDD DDD.

**Cucumber BDD** : +2 scénarios (total 16/16 PASS)
- `Plugin adds cucumber dependencies to testImplementation` (US-CN-7.1) : 4 Then steps
  - cucumber-java, cucumber-junit-platform-engine, junit-platform-suite, workspace-bom platform
- `Plugin adds junit test dependencies` (US-CN-7.2) : 6 Then steps
  - kotlin-test-junit5, junit-jupiter, junit-platform-params, assertj-core, junit-platform-launcher (runtime), workspace-bom platform

**Functional tests (GradleTestKit)** : inchangés (15/15 PASS) — pas de nouveaux tests fonctionnels, les scénarios Cucumber couvrent l'inspection des deps via `dependencies --configuration testImplementation`.

**Nouveau fichier testé** : `TestDependencies.kt` (82 lignes)
- Couverture indirecte via les 2 plugins consommateurs (CucumberConventionsPlugin, GradlePluginConventionsPlugin)
- 5 deps cucumber + 5 deps junit + platform BOM vérifiées par les steps Cucumber

**Migration plantuml-gradle (dogfood CNV-7.2)** :
- `compileKotlin` SUCCESS
- `:test` 956 tests exécutés (bug `6.0.3` éliminé — zéro `NoSuchMethodError` dans rapports)
- 5 failures = tests LLM Ollama timeout réseau 30s (préexistants, indépendants de CNV-7)

**Couverture cumulée** : 16/16 Cucumber + 15/15 functional = 31 tests PASS.

== Session 011

**CNV-8 Linter Kotlin + Configuration Cache** — 4 US (10 pts) en baby-step TDD/BDD DDD.

**Functional tests (GradleTestKit)** : +4 tests (total 20/20 PASS)
- LintConventionsFunctionalTest
  - `plugin applies without error` : ✅
  - `plugin registers ktlint tasks` : ✅
  - `plugin registers detekt task` : ✅ (adaptatif Java 25)
  - `check task depends on detekt` : ✅ (adaptatif Java 25)

**Cucumber BDD** : +2 scénarios (total 21/21 PASS)
- `Plugin applies ktlint` : ✅ (1 Then : ktlint plugin applied)
- `Plugin registers ktlint tasks` : ✅ (1 Then : ktlint tasks available)

**Scénarios detekt retirés** (2) : `Plugin applies ktlint and detekt` et `Plugin registers detekt task` retirés car detekt est skip sur Java 25. Réintroduire quand detekt 2.0+ sera disponible sur Maven Central.

**Couverture cumulée** : 21/21 Cucumber + 20/20 functional = 41 tests PASS.
