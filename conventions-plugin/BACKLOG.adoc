= Backlog — Conventions Gradle Plugin
:description: Backlog prioritaire — EPICs CNV-0→CNV-6 + PUB
:toc: left
:toclevels: 4
:icons: font
:sectnums:

_Derniere mise a jour_ : 2026-07-31 — CNV-9 ✅ TERMINÉ (session 012)

'''

== EPICs Termines

[cols="1,3,2,3"]
|===
| EPIC | Description | Pts | Cloture
| CNV-0 | Bootstrap gouvernance — .agents/, AGENT.adoc, INDEX.adoc, BACKLOG, CODE_REVIEW_GLOBALE | — | Sess. 000
| CNV-1 | Convention plugin `gradle-plugin` — java-gradle-plugin + kotlin-jvm + test JUnit + withSourcesJar/JavadocJar | 8 | Sess. 001
| CNV-2 | Convention plugin `publishing` — POM Maven Central + signing conditionnel + relocation + publicationType | 6 | Sess. 002
| CNV-3 | Convention plugin `functional-test` — source set + GradleTestKit + task + check.dependsOn + TOML catalog + programmatic access | 5 | Sess. 003
| CNV-4 | Convention plugin `cucumber` — features/scenarios dirs + cucumberTest task + test filter + check.dependsOn + CucumberTaskSpec multi-task | 6 | Sess. 004
|===

== ✅ TERMINE — CNV-5 : Migration Dogfood (3 projets)

_Validee sur 3 profils differents — pret pour generalisation._

*Priorite* : P1 — ✅ TERMINE
*Charge estimee* : 10 pts (~3 sessions)
*Depend de* : CNV-1, CNV-2, CNV-3, CNV-4

=== Projets dogfood migres

[cols="1,4,3,2"]
|===
| Projet | Profil | Verdict | Session
| dashboard-gradle | NOUVEAU | ✅ 141→33 lignes, 34 tests PASS | 005
| codebase-gradle | EXISTANT MATURE | ✅ 356→301 lignes, compile + test compile OK | 006
| capsule-gradle | EXISTANT LEGACY | ✅ 131→80 lignes, compile + functional tests OK | 005
|===

=== User Stories

[cols="1,4,2,2"]
|===
| US | Description | Pts | Statut
| US-5.1 | Migrer dashboard-gradle vers les 4 convention plugins | 3 | ✅ Sess. 005
| US-5.2 | Migrer codebase-gradle (verifier compat 32 cucumber tasks) | 4 | ✅ Sess. 006
| US-5.3 | Migrer capsule-gradle (verifier config incomplete + legacy) | 3 | ✅ Sess. 005
|===

'''

== A FAIRE — CNV-6 : Bugs fixes + Generalisation

_Bugs fixes (runnerClass, mavenLocal, extendsFrom, cucumberTest name) — TDD 100% — v0.0.1 published_

*Priorite* : P1 (fixes) / P2 (generalisation)
*Charge estimee* : 8 pts fixes + 15 pts generalisation
*Depend de* : CNV-5
*Statut* : Fix terminés, generalisation en cours

=== Projets cibles pour generalisation (par ordre de priorite)

1. bakery-gradle (projet gold standard)
2. plantuml-gradle
3. slider-gradle
4. readme-gradle
5. hyperframes-gradle
6. graphify-gradle
7. api-key-pool-gradle
8. planner-gradle
9. codex-gradle
10. agile-gradle, ticket-gradle, review-gradle, flow-gradle
11. runner-gradle, document-gradle

'''

== ✅ TERMINE — PUB : Publication

_Publication des convention plugins sur Gradle Plugin Portal + Maven Central — 3/4 US terminées._

*Priorite* : P1
*Charge estimee* : 7 pts (~2 sessions)
*Depend de* : CNV-5 (tous les plugins valides sur 3 profils)
*Statut* : ✅ TERMINE (3/4 US — US-PUB-4 doc reste P2)

=== User Stories

[cols="1,4,2,2"]
|===
| US | Description | Pts | Statut
| US-PUB-1 | Configurer sonatype + signing pour `education.cccp.build` | 2 | ✅ Sess. 008
| US-PUB-2 | Publier sur Maven Central (nmcp) | 2 | ✅ Sess. 008
| US-PUB-3 | Publier sur Gradle Plugin Portal | 1 | ✅ Sess. 008
| US-PUB-4 | Documenter la migration dans AGENT.adoc de chaque borough | 2 | 🟡 RESTANT
|===

'''

== ✅ TERMINE — CNV-7 : Centralisation des dépendances de test

_Dépendances JUnit + Cucumber gérées par les convention plugins — élimine la duplication et les bugs de version dans les catalogues locaux des boroughs._

*Priorité* : P1 — débloque US-I18N-6 plantuml (tests `:test` cassés par `junit-platform-params = "6.0.3"` dans catalogue local)
*Charge estimée* : ~8 pts (~2 sessions)
*Dépend de* : CNV-4 (CucumberConventionsPlugin existe), CNV-3 (FunctionalTestConventionsPlugin existe)
*Statut* : ✅ TERMINÉ (session 010, 2026-07-05)

=== Contexte

Découvert session plantuml 158 : `junit-platform-params = "6.0.3"` (version inexistante)
dans `plantuml-plugin/gradle/libs.versions.toml` casse l'exécution des tests
(`NoSuchMethodError: CollectionUtils.toUnmodifiableList()`).

Racine : les convention plugins ne géraient **pas** les dépendances de test.
Tous les boroughs (bakery, capsule, plantuml…) dupliquaient
`testImplementation(kotlin("test-junit5"))`, `junit-platform-launcher`,
`libs.bundles.cucumber`, `junit-platform-params` dans leur `build.gradle.kts`
local avec des versions hardcodées dans leur catalogue — d'où les divergences
et bugs.

=== Architecture cible (RÉALISÉE)

* `GradlePluginConventionsPlugin` (via `TestDependencies.addJunitDeps`) :
  - `testImplementation` : `kotlin("test-junit5")`, `junit-jupiter` (fallback `5.12.2`),
    `junit-platform-params` (fallback `5.12.2`), `assertj-core` (fallback `3.27.7`)
  - `testRuntimeOnly` : `junit-platform-launcher` (fallback `1.14.3`)
  - `workspace-bom` platform sur `testImplementation` + `testRuntimeOnly`

* `CucumberConventionsPlugin` (via `TestDependencies.addCucumberDeps`) :
  - `testImplementation` : `cucumber-java`, `cucumber-junit-platform-engine`,
    `cucumber-picocontainer` (fallback `7.34.3`), `junit-platform-suite` (fallback `1.14.3`)
  - `workspace-bom` platform
  - `addFromCatalog` pattern existant (lit catalogue local, fallback hardcoded)

* Helper DDD `TestDependencies.kt` (single responsibility = gestion deps test) :
  - `addCucumberDeps`, `addJunitDeps`, `addPlatformBom`, `addFromCatalog`, `libs`
  - Constantes `CUCUMBER_FALLBACKS`, `JUNIT_FALLBACKS`, `WORKSPACE_BOM_COORDINATES`
  - Consommateurs : CucumberConventionsPlugin, GradlePluginConventionsPlugin, FunctionalTestConventionsPlugin (refactor)

=== User Stories

[cols="1,4,2,2"]
|===
| US | Description | Pts | Statut

| US-CN-7.1
| **CucumberConventionsPlugin : deps cucumber** — Ajouter `cucumber-java`,
  `cucumber-junit-platform-engine`, `cucumber-picocontainer` (fallback `7.34.3`),
  `junit-platform-suite` (fallback `1.14.3`) via `addFromCatalog` +
  `workspace-bom` platform. TDD : tests unitaires vérifiant les deps ajoutées.
| 3
| ✅ Sess. 010

| US-CN-7.2
| **GradlePluginConventionsPlugin : deps junit test** — Ajouter
  `kotlin("test-junit5")`, `junit-jupiter` (fallback `5.12.2`),
  `junit-platform-params` (fallback `5.12.2`), `assertj-core` (fallback `3.27.7`)
  en `testImplementation` + `junit-platform-launcher` (fallback `1.14.3`)
  en `testRuntimeOnly` + `workspace-bom` platform. TDD.
| 3
| ✅ Sess. 010

| US-CN-7.3
| **Migration plantuml-gradle** — Appliquer `CucumberConventionsPlugin` +
  `FunctionalTestConventionsPlugin`, supprimer les deps test locales du
  `build.gradle.kts` et les entrées cucumber/junit du catalogue local.
  Le bug `6.0.3` disparaît naturellement. `compileKotlin` + `:test` SUCCESS.
| 2
| ✅ Sess. 010
|===

=== Critères d'Acceptation

* [x] `CucumberConventionsPlugin` ajoute les deps cucumber via `addFromCatalog`
* [x] `GradlePluginConventionsPlugin` (ou `TestConventionsPlugin`) ajoute les deps junit
* [x] `addFromCatalog` lit le catalogue local, fallback hardcoded correct
* [x] `workspace-bom` platform appliqué sur `testImplementation` + `testRuntimeOnly`
* [x] plantuml-gradle migré : zéro dep test locale dans `build.gradle.kts`
* [x] plantuml `:test` exécute (plus de `NoSuchMethodError`)
* [x] `compileKotlin` SUCCESS sur conventions + plantuml
* [x] `publishToMavenLocal` SUCCESS sur conventions

'''

== Archives

Session 005 (ARCH) : Architecture "Plugin Independant + Racine Consommateur".
`conventions-plugin/` est maintenant un build Gradle completement independant avec son propre wrapper, catalog, et settings.
Root `settings.gradle.kts` : plus de `include()`. Root `build.gradle.kts` : consommateur dogfood via `mavenLocal()`.
Workflow : `cd conventions-plugin && ./gradlew publishToMavenLocal && cd .. && ./gradlew build`

'''

== A FAIRE — CNV-10 : Test Coverage Gaps (TDD rétroactif)

_Gaps de couverture identifiés session 012 — audit complet des 6 plugins + TestDependencies._

*Priorite* : P0 — 0 test unitaire, branches internes non couvertes, feature file désynchronisé
*Charge estimee* : ~14 pts (~2-3 sessions)
*Depend de* : CNV-9

=== Gaps par plugin

[cols="1,3,2,2"]
|===
| # | Gap | Pts | Impact
| US-CN-10.1
| **GradlePluginConventionsPlugin** — `configureBuildCache` non testé, `configureRepositories` non testé, feature file vérifie Java 24 mais le code compile Java 25 (désynchro)
| 3
| Tous les boroughs

| US-CN-10.2
| **PublishingConventionsPlugin** — `publicationType = "LIBRARY"` (fallback website/vcsUrl) non testé
| 2
| graphify, api-key-pool

| US-CN-10.3
| **FunctionalTestConventionsPlugin** — `extendsFrom(testImplementation)` non testé, `additionalDependencies` non testé
| 2
| 12+ boroughs

| US-CN-10.4
| **CucumberConventionsPlugin** — `parallel`, `timeoutMinutes`, `cucumberTestTaskName` configurable, `additionalTasks.features/tags` non testés
| 3
| 8+ boroughs

| US-CN-10.5
| **LintConventionsPlugin** — branche detekt (non-Java-25) jamais exécutée, zéro test unitaire
| 2
| bakery

| US-CN-10.6
| **LogbackExclusionConventionsPlugin** — zéro functional test, seulement 2 scénarios Cucumber
| 1
| bakery, codex, document

| US-CN-10.7
| **TestDependencies** — fallback hardcoded (catalogue absent), `addPlatformBom` exception path non testés
| 1
| Tous les boroughs
|===

=== Problème structurel

*Zéro test unitaire* (`src/test/kotlin/` est vide). Tout repose sur functional + Cucumber
(testent l'intégration mais pas les branches internes). Baby-step TDD/BDD DDD exige
les 3 niveaux : unit → functional → cucumber.

=== Critères d'Acceptation

* [ ] `src/test/kotlin/` contient des tests unitaires pour chaque plugin
* [ ] `configureBuildCache` testé unitairement
* [ ] `configureRepositories` testé unitairement
* [ ] Feature file `gradle-plugin-conventions.feature` corrigé Java 24 → 25
* [ ] `publicationType = "LIBRARY"` testé (functional + cucumber)
* [ ] `extendsFrom(testImplementation)` testé (functional)
* [ ] `additionalDependencies` testé (functional)
* [ ] `parallel`, `timeoutMinutes`, `cucumberTestTaskName` testés (cucumber)
* [ ] `additionalTasks.features/tags` testés (cucumber)
* [ ] Branche detekt testée (mock Java version)
* [ ] `LogbackExclusionConventionsPlugin` a un functional test
* [ ] `TestDependencies` fallback hardcoded testé
* [ ] `addPlatformBom` exception path testé

'''
== Synthese — Charge restante

[cols="1,3,2,2"]
|===
| EPIC | Contenu | Pts | Priorite
| CNV-0 | Bootstrap gouvernance | — | ✅ TERMINE
| CNV-1 | Convention plugin `gradle-plugin` | 8 | ✅ TERMINE
| CNV-2 | Convention plugin `publishing` | 6 | ✅ TERMINE
| CNV-3 | Convention plugin `functional-test` | 5 | ✅ TERMINE
| CNV-4 | Convention plugin `cucumber` | 6 | ✅ TERMINE
| CNV-5 | Migration dogfood 3 projets | 10 | ✅ TERMINE
| CNV-6 | Generalisation 15+ projets | 15 | ✅ TERMINE
| CNV-7 | Centralisation deps test (junit + cucumber) | 8 | ✅ TERMINE
| CNV-8 | Linter Kotlin + Configuration Cache | 10 | ✅ TERMINE
| CNV-9 | Publication 0.0.3 Maven Central + Plugin Portal + doc boroughs + bump BOM | 6 | ✅ TERMINE
| CNV-10 | Test Coverage Gaps — TDD rétroactif (7 gaps, 0 unit test) | 14 | 🔴 P0
| PUB | Publication Plugin Portal + Maven Central | 7 | ✅ TERMINE
| *Total* | | *95* |
|===
